[user]
	email = emallson@archlinux.us
	name = J David Smith
[core]
	editor = emacsclient -c
	excludesfile = ~/.gitignore_global
[push]
	default = matching
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
